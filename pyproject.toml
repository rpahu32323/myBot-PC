[project]
name = "mybot-pc"
version = "2601"
description = "myBot PC application"
readme = "README.md"
authors = [
    { name = "rpahu32323", email = "rpahu32323@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
mybot-pc = "mybot_pc:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
